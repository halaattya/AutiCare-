import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFFFFF6E9); // soft cream
  static const cardLight = Color(0xFFFFEEC3);  // soft yellow
  static const cardPeach = Color(0xFFFFD9B3);  // warm peach
  static const cardBlue = Color(0xFFCFE8FF);   // baby blue
  static const cardLavender = Color(0xFFE8D9FF); // soft lavender

  // Accents
  static const accentPeach = Color(0xFFF5A971);
  static const accentBlue = Color(0xFF82B8FF);

  // Text
  static const textDark = Color(0xFF243240);
  static const textSoft = Color(0xFF6B7280);

  // Borders / subtle
  static const borderSoft = Color(0xFFE5D3C3);
}

